COMMON_FLAGS="-march=native -O3 -flto=thin -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

LD="ld.lld"
CC="clang"
CPP="clang-cpp"
CXX="clang++"
AR="llvm-ar"
NM="llvm-nm"
RANLIB="llvm-ranlib"

LDFLAGS="${LDFLAGS} -fuse-ld=lld -rtlib=compiler-rt -unwindlib=libunwind -Wl,--as-needed -Wl,-O3 -Wl,--thinlto-jobs=12"

RUSTFLAGS="-C target-cpu=native -C opt-level=3"

MAKEOPTS="-j12 -l13"
EMERGE_DEFAULT_OPTS="--jobs=2 --load-average=11"
FEATURES="candy parallel-fetch parallel-install"
PORTAGE_NICENESS="10"

USE="X dbus vulkan opengl alsa pipewire lto jit llvm clang -doc -gnome -cups -telemetry"

ACCEPT_LICENSE="*"
ACCEPT_KEYWORDS="~amd64"

LC_MESSAGES=C.UTF-8
L10N="ru"

CPU_FLAGS_X86="aes avx avx2 avx_vnni bmi1 bmi2 f16c fma3 mmx mmxext pclmul popcnt rdrand sha sse sse2 sse3 sse4_1 sse4_2 ssse3 vpclmulqdq"
VIDEO_CARDS="amdgpu radeonsi"
INPUT_DEVICES="libinput"
GRUB_PLATFORMS="efi-64"

GENTOO_MIRRORS="https://mirror.yandex.ru/gentoo-distfiles/"
